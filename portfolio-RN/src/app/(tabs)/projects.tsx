/// Projects tab entry point — delegates to feature screen.
import { ProjectsScreen } from '@/features/projects/screens/ProjectsScreen';

export default function ProjectsTab() {
  return <ProjectsScreen />;
}
