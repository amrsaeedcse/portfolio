/// Contact tab entry point — delegates to feature screen.
import { ContactScreen } from '@/features/contact/screens/ContactScreen';

export default function ContactTab() {
  return <ContactScreen />;
}
